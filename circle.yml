machine:
  node:
    version: 6.1.0
test:
  pre:
    - npm run lint
  override:
    - npm test -- --coverage
deployment:
  master:
    branch: master
    commands:
      - ./scripts/deploydoc.sh
general:
  branches:
    ignore:
      - /.+-experimental/